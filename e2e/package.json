{
  "name": "lazysheet-e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "wdio run ./wdio.conf.js",
    "coverage": "nyc merge ../.nyc_output ../coverage/merged.json && nyc report -t ../.nyc_output --reporter=text-summary --reporter=html --report-dir ../coverage/html && nyc check-coverage -t ../.nyc_output --lines 95"
  },
  "devDependencies": {
    "@wdio/cli": "^9",
    "@wdio/local-runner": "^9",
    "@wdio/mocha-framework": "^9",
    "@wdio/spec-reporter": "^9",
    "nyc": "^17.0.0"
  }
}
