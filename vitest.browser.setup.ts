// Browser-mode setup: import Tailwind v4 CSS so computed-style assertions
// in responsive e2e tests read real layout values.
// Use a relative path — the '#' alias may not resolve during setup file execution.
import './src/styles.css'
